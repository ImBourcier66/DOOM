# DOOM Game Project

This is a simple implementation of the DOOM game.

## Building the Project

To build the project, use the following command:

```bash
mkdir build
cd build
cmake ..
make
```

## Running the Game

After building, run the game executable located in the build directory.
